import styled from "styled-components"
import { colors } from "styles/GlobalStyle"

export const Section = styled.section`
  background-color: ${colors.grey};
  box-shadow: 0px 4px 4px ${colors.blue};
  margin: 0 5px;
  padding: 1.2rem;
`